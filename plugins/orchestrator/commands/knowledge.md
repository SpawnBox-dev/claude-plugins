---
description: "Browse the orchestrator's knowledge graph - search, filter by type/domain, inspect notes"
---

Ask the user what they want to explore:
1. Search by topic (use `recall` with query)
2. Browse by type (use `recall` with type filter)
3. View autonomy scores (use `reflect`)
4. View user model (use `recall` with type "user_pattern")

Present results interactively - let the user drill into specific notes by ID.
